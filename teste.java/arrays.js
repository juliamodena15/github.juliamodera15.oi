// exc.1 facil 

console.log(Math.max(1, 190, 7, 20))

// exc.2 

const lista = [
    'Cozinha',
    'Banheiro',
    'Sala',
    'Quarto',
    'Lavanderia',
]

if (lista.includes('Sala de Jantar')) {
    console.log(`Essa lista contém este item`);
}
else {
    console.log(`Essa lista não contém este item`);
}

// exc.3

const reverso = lista.reverse();
console.log('reversed:',reverso)

// exc.4

